# 2026-09-22 工作日志

## Git 提交冻结（为后续文件整理建立回退点）

**目标**：先把全部工作区内容提交并推送，使后续清理（删目录、拆文档）都能通过 Git 回退。

**结果**：553 个文件（15 个已修改 + 538 个未跟踪）分为 6 个语义化提交，已推送到私有远端。

| 提交 | 内容 | 文件数 |
| --- | --- | --- |
| `89d0da7` | 项目技能：运营规则拆分硬约束/启发式 | 10 |
| `3d27bbb` | 入口文档、唯一 SOP 与记忆索引 | 8 |
| `ffe6d63` | Alibaba 运营证据（BQ 系列 / HR 系列 / 直通车） | 468 |
| `9009c1a` | 客户开发与主动获客 | 28 |
| `9649f01` | 工具链脚本与 Hypit 接入 | 36 |
| `4e29cc5` | 竞品研究与待清理副本目录 | 3 |

- 远端：`https://github.com/421345308/beiqiang-cross-border-operations.git`
- `156521a..4e29cc5  main -> main`，本地 HEAD 与 origin/main 一致，工作区未提交项 = 0

## 本次确认的环境事实（重要）

1. **远端仓库确认为私有**：匿名 `git ls-remote` 被拒（要求用户名），公开 GitHub API 返回 404。无需再改可见性设置。
2. **沙箱内 git 网络操作被拦截**：`git ls-remote` / `git push` 在沙箱内静默失败（exit 128、无任何输出），本地读写命令正常。推送必须用沙箱外执行。诊断要点：先测公开仓库（`github.com/git/git`）能否匿名拉取，以区分「网络问题」与「认证问题」。
3. **凭据走 Git Credential Manager**（`credential.helper=manager`，系统级），无存储 token，`gh` CLI 未安装。
4. 沙箱内不支持 `Start-Job`、`cmd /c`。
5. 提交中文信息用 `git commit -F <UTF-8 文件>`，避免控制台编码问题；`git log` 中文需先把 `[Console]::OutputEncoding` 设为 UTF8，否则显示为乱码（并非仓库内损坏）。
6. `git status --porcelain` 过滤注意：`-notlike '??*'` 会误伤 ` M`（空格+M）行，须用 `$_.Substring(0,2) -eq '??'`。

## 安全扫描结论

- 项目自身秘密信息启发式扫描 23 处命中**全部为误报**（Python 变量名 `token`、文档中的 credentials/authentication 说明文字）。
- 真实凭据在 `~/.config/beiqiang/alibaba-openapi.json`（仓库外），设计正确。
- 待提交文件中仅 1 个 >5MB：`04_客户开发/01_线索与CRM/outputs/.../主动获客_规模化流水线_2026-09-17.xlsx.inspect.ndjson`（16.16 MB，xlsx 的派生导出；因源 xlsx 未入库，保守起见一并提交）。

## 仓库体积观察

`git count-objects -vH`：total 760.55 MiB / pack 339.48 MiB / garbage 52.07 MiB / prune-packable 771。
pack 偏大说明**历史提交中仍存在早期纳入的媒体与 Excel blob**（09-11 整理时只把文件移出索引，blob 仍留在历史）。未执行 gc，待负责人决定是否做历史瘦身。

## 文件整理（已落盘，提交待 Codex 释放 git 锁）

按顺序执行完毕，4 项全部完成：

1. **拆分 `当前状态.md`**：51,367 字节 / 160 行追加式流水 → 8,654 字节现状快照，最长行 742 → 254 字符。原流水字节级快照归档到 `90_归档/总控历史/当前状态流水_2026-09-08至2026-09-21.md`，源哈希 `55D6D762…` 记录在文件头。
2. **清理残留**：`git rm` 删除错别字目录 `05_内容与见频/`；11 个 Remotion 顶层空壳移入 `99_临时区/待清理_2026-09-22/remotion顶层空壳/`。`.agents/skills` 回到 **21 个目录**，与 Skills 清单一致。
3. **临时区保留期**：定为 30 天（自最后写入起算），超期仅进「待清理候选」，须负责人确认 + 三条清理条件。写入 `99_临时区/README.md`；26 个根层散件归入对应 `analysis_20260816_BQ03x/`；删除空目录 `skill_update_20260817/`。
4. **修正偏差 + 自检**：`03_Skills清单.md` 记录 Remotion 空壳的实际移除过程；`check_workspace.py` 将 `.workbuddy` 与 `outputs` 列为工具管理根条目；把 D7 工作簿从 `outputs/` 归位到 BQ 主线目录。**自检 PASS：0 error 0 warning**（skills=21、memory=15、tracked=2078）。

## 阻塞：Git 提交未完成

`git add` 返回 128，原因：**Codex 会话持有 `.git/index.lock`**（检查时该锁仅 168 秒龄，属活跃锁，非残留）。

- **不得手工删除 index.lock** —— 会破坏 Codex 正在进行的操作。项目历史里 9/7 那次清锁是因为已确认无活动进程，本次不适用。
- 本次**刻意未暂存** Codex 正在写的 `02_Alibaba运营/05_扩品工程/HR系列真实货源改造_2026-09-21/`，尊重「不覆盖其他任务未提交改动」。
- Codex 在我重写 `当前状态.md` 之后又向其追加了内容（8,654 → 9,147 字节），**我的版式完好保留，未发生冲突**。
- 待 Codex 结束、锁释放后：暂存以下范围并推送——
  `00_总控台/当前状态.md`、`00_总控台/工作区维护.md`、`07_知识库与Skills/03_Skills清单.md`、`08_工具链/06_工作区维护/check_workspace.py`、`90_归档/总控历史/`，以及已暂存的 `05_内容与见频` 删除。提交信息见 `99_临时区/_mA.txt`。

## 仍待处理（下一阶段）

- `_codex_work/`（33,638 文件 / 1.58 GB）仍在根目录，文档说明 Blender MCP 在用
- 仓库 pack 339 MB，历史含早期纳入的媒体/Excel blob；`.git` 有 52 MB 垃圾对象与 771 个可回收对象 —— 未执行 gc，待负责人决定是否做历史瘦身
- `99_临时区/` 5 个目录已超 30 天保留期（38/37/37/37/30 天），列在 `99_临时区/README.md`，待确认后清理
- `多链接首批视觉检查/` 为空目录，回收站策略对非 ASCII 路径失败，未能删除
- `04_客户开发/01_线索与CRM/outputs/` 为嵌套 outputs，含 16 MB 派生检查缓存，建议同样归置

## index.lock 事件（需记取教训）

09:23 起 `.git/index.lock` 存在近 10 小时，一度让我误判为 Codex 持有。复查发现：

- 锁的时间戳 09:23:45 正是**我自己**跑清理提交脚本的时刻，当时 PowerShell 进程发生过访问冲突崩溃（exit `-1073741819`），`git add` 被中断，锁未清除。
- 19:04 检查时无任何 git 进程在运行，锁为 **0 字节** → 确认是残留锁，按项目 9/7 先例清除，git 恢复可用。
- **教训**：脚本化 git 操作应逐条检查退出码；出现进程崩溃后必须复查 `index.lock`，不能想当然归因于并行的 Codex 会话。

## awesome-seedance 引入（L1 + L2）

**定位纠正**：负责人指出工作区是「个人所有项目的总和」，鞋业只是其中一部分，不能用「对卖鞋有没有用」裁剪通用能力。我原先的 25 模板判定把 13 个判为「不适用」是错的框架。

按新定位落盘：

| 层 | 产出 | 是否入库 |
| --- | --- | --- |
| L1 参考 | `08_工具链/02_视频工具/awesome-seedance/`（克隆，153 文件 / 11.01 MB） | 否，已加 `.gitignore` |
| L1 索引 | `07_知识库与Skills/04_AI视频知识体系/awesome-seedance-模板索引.md`（25 模板全量，工作区级） | 是 |
| L2 吸收 | `.agents/skills/beiqiang-seedance-video/references/template-mapping.md`（鞋业场景子集 + 改造方向 + 场景红线） | 是 |
| L2 挂钩 | `prompt-framework.md` 顶部加模板库指引；`08_工具链/02_视频工具/README.md` 登记来源与许可 | 是 |

核心概念：**约束属于使用场景，不属于模板。** `ugc-creator-review` 用在虚构创作里合法，用在贝强对外商品视频里假装真实买家评测才违规。

关键数据：25 模板 = 结构基础 3 + 写实 UGC 4 + 商业产品 4 + 叙事表演 5 + 风格动画 3 + 动作特效 6。许可为代码 MIT / 策展 CC BY 4.0 / 提示词与媒体归原作者，三套并存。

自检 PASS（0 error / 0 warning）。改动尚未提交，待 Codex 收工后按范围提交（只提交自己这部分，Codex 的 HR 系列文件不动）。


