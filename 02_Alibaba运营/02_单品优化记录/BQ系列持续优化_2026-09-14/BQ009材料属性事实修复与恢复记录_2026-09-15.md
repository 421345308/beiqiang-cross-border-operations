# BQ009材料属性事实修复与恢复记录（2026-09-15）

## 1. 触发原因

BQ009-O1买家公开页显示中底和外底为`EVA, Confirm Before Bulk Order`。回查`01_产品资产/03_技术资料卡/BQ009_L1026_技术资料卡.md`及最终上架填写表后确认：BQ009的中底材料和接地外底材料均待供应链或样品确认，不能写成EVA。源款`1601825074604`继续永久只读，只处理三条扩展链接。

## 2. 实时Schema与参考证据

- 类目`201334413`当前Schema显示Midsole Material与Outsole Material均必填，并提供`Other`选项；Schema查询请求ID：`213c932b17894219716213800`。
- 平台已存在且能正常回读的参考商品`10000047392343`，两项`Other`在渲染Schema中实际编码为`<value inputValue="Other">4</value>`；参考渲染请求ID：`21501b6717894223440704544`。
- 因此最终目标值冻结为`value_id=4 / inputValue=Other`，不得使用`-1`空选项语义，也不得猜测EVA、Rubber、TPR或其他具体材料。

## 3. 第一次写入与异常

第一次按照类目Schema静态option的`value=-1`提交，接口受理，但即时回读显示两项必填材料被删除而不是写为Other。三条均进入`modified/N`；标题、六图、44个SKU及全部非目标字段保持不变，源款未触碰。

| Product ID | 更新请求ID | 即时回读请求ID | 当前状态 | 异常 |
| --- | --- | --- | --- | --- |
| `1601939661419` | `213e759c17894221615682968` | `213f5a2117894221697951511` | modified/N | Midsole与Outsole为空 |
| `1601939675350` | `212cbf3217894221941335876` | `0b80d2d617894222021836558` | modified/N | 同上 |
| `1601939721165` | `21501b8d17894222267035853` | `213d4e4917894222349133599` | modified/N | 同上 |

该结果记录为失败，不能计为修复完成。平台在审核中拒绝再次渲染/提交，错误为`PUB_BIZCHECK_PRODUCT_IN_AUDITING`。没有重复写入。

审核期间再次打开BQ009-O1买家URL，公开页仍可正常访问并显示原标题，说明旧公开版本尚在服务买家；不能把`product.get modified/N`直接解释为公开链接已经下线。新材料值是否生效仍必须等待审核后看公开页。

## 4. 已准备恢复动作

- 修复工具：`08_工具链/01_业务脚本/商品图片与审计/repair_bq009_unknown_materials.py`。
- 工具已改用平台实证编码`4 + inputValue=Other`，并保留源款硬拦截、型号/类目/六图校验和非目标字段快照比较。
- 审核状态恢复可写后，先逐条只读回读。若旧值回滚，或两项仍为空，则以`Other`恢复；若已经是Other，只记录`verified_existing`，不重复提交。
- 恢复后必须依次核验API回读、审核终态和买家公开页；公开页材料字段显示Other、标题/M1/六图/SKU/价格不变后才转回`PUBLIC_QA_PASS`。

## 5. 当前经营保护

- BQ009三条扩展链接暂时标记`SUBMITTED`，优先级高于D7复盘。
- BQ009-O1仍保留`PROTECT_TRAFFIC_ENTRY`语义；材料事实修复不能被解释为允许改标题或首图。
- BQ009/L1026源款`1601825074604`不在任何写入目标中，继续承担现有流量和询盘承接。

## 6. 正确值恢复提交

审核锁解除后三条先恢复为`approved/Y`但材料仍为空。R1先单条预检并提交，确认`Other / value_id 4`即时回读成功且非目标字段未变；随后才按相同冻结载荷顺序处理W1、O1。

| Product ID | 恢复更新请求ID | 即时回读请求ID | Midsole | Outsole | 非目标字段 |
| --- | --- | --- | --- | --- | --- |
| `1601939661419` | `210829b717894231511038164` | `0b51802d17894231594583887` | Other / 4 | Other / 4 | 全部不变 |
| `1601939675350` | `21501b6717894232067466045` | `213c934417894232147913300` | Other / 4 | Other / 4 | 全部不变 |
| `1601939721165` | `21501be117894232518671004` | `212c41d317894232599375783` | Other / 4 | Other / 4 | 全部不变 |

三条当前再次进入`modified/N`审核，但这次必填材料即时回读已经正确。禁止重复提交；审核恢复后核验`approved/Y`、两项Other以及买家公开页，再关闭恢复任务。

## 7. 2026-09-15 复核轮询

- 只读轮询回执：`BQ009材料审核轮询_2026-09-15.json`。
- R1/W1/O1三条仍为`modified/N`，审核尚未结束；本轮未调用任何更新接口。
- 三条均继续稳定回读Midsole Material=`Other / 4`、Outsole Material=`Other / 4`，标题、六图与44个SKU保持不变。
- 本轮请求ID：R1 `0bab02fc17894234294987148`、W1 `0bbb637a17894234381475543`、O1 `213e759c17894234467166289`。
- 当前动作保持`WAIT_RECOVERY_REVIEW`：禁止重复提交；只有恢复`approved/Y`后才进入公开买家页终验并转回`PUBLIC_QA_PASS`。

## 8. 审核终态与公开页验收

- 第三次只读回读确认R1/W1/O1全部恢复`approved/Y`，两项材料继续为`Other / value_id 4`，六图与44 SKU正常。
- Chrome逐条打开三个独立买家URL并视觉检查：R1为Mint Green完整鞋体，W1为Orange完整鞋体，O1为All Black三分之四完整鞋体；标题、四色、EU35–45、询盘入口均正常。
- 三条公开页的核心属性区和重要属性区均显示Outsole Material=`Other`、Midsole Material=`Other`，错误EVA声明已不再传播。
- 三条已由`SUBMITTED`转回`PUBLIC_QA_PASS`；R1/W1恢复原D7节奏，已有点击的O1继续`PROTECT_TRAFFIC_ENTRY`至D14。
- 结构化证据见`BQ009材料恢复公开页终验回执_2026-09-15.json`。BQ009/L1026源款`1601825074604`全程未修改。
