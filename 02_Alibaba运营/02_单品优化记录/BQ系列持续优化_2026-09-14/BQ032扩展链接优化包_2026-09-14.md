# BQ032 / ZX2212 扩展链接优化包

## 决策

- 源链接 `10000046653813` 为原始款，只读保护，不修改。
- 仅优化 W1 `1601939651515`、R1 `1601939751023`、O1 `1601939722196` 三条扩展链接。
- 三条均为同一真实鞋型的不同搜索入口，不把颜色变化解释成新鞋型；核心目标分别是轻量批发、EU47大码日常、全黑定制采购。
- BQ032 为 `Regular Fit`，不使用 `Wide Toe Box`；不写防水、矫形、跑鞋或未经测试的防滑承诺。

## 标题与关键词逻辑

- W1：以 `Wholesale + Mens Lightweight Walking Shoes` 承接批发与轻量步行搜索。
- R1：删除原题中的 `Large Size for Large Size` 重复，前置 `Mens Large Size Walking Shoes`，用已确认 EU38–47 支撑大码意图。
- O1：首图采用真实全黑色，标题以 `All Black + Custom Logo OEM ODM` 对齐私牌采购意图。
- 近7日关键词证据中，`mens lightweight walking shoes` 搜索指数7、趋势+50%、CTR 13.33%；`mens shoes size 47` 指数5、趋势+300%；`mens shoes casual` 指数19、CTR 13.79%。数据量仍小，因此用于方向优先级，不当作稳定结论。
- `productKeywords` 当前 OpenAPI 字段在 BQ031 已出现“提交成功但回读为空”，本批不冒险重复调用；先将有效词自然写入标题前半段，后台关键词字段只在验证出可靠UI链路后单独处理。

## 主图与六图

- 第一版因残留颜色标签、主体偏小被驳回，没有上传。
- 第二版从同款六张真实颜色图中只截取完整鞋对并放大到 1000×1000；无拼图、无AI改色、无鞋型/鞋底/闭合结构改动，六张已逐张视觉复核。
- W1 首图蓝色、R1 首图杏色、O1 首图全黑；其余五图重新排列，让搜索列表先展示不同真实颜色，同时完整呈现六个可售颜色。

## 提交与验收

1. 源产品ID写入四个更新工具的硬保护集合。
2. 先跑只读门禁：型号、审核状态、六图、60 SKU、文件存在、标题长度、源款未命中。
3. 只在三条扩展链接均为 `approved/Y` 时提交标题与完整六图。
4. 回读标题、六图URL、型号、SKU和所有非目标字段；审核中不重提。
5. 恢复 `approved/Y` 后，买家页逐条检查首图、六图、六色、EU38–47、型号、三档价格、Pairs、Lace-Up与Regular Fit。
