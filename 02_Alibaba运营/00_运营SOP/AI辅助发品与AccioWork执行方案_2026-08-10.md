# AI 辅助发品与 Accio Work 执行方案

## 结论

AI 可以帮助贝强完成约 80%-90% 的发品准备工作；Accio Work / Work Agent 从能力设计上可以处理商品查询、素材包解析、类目预测、属性/SKU 映射、预检、草稿和发布任务。但最终发布必须保留人工确认关口，尤其是材质、尺码、价格、MOQ、包装、交期、定制和物流字段。

## 推荐工作流

### 阶段 1：AI 整理本地数据包

1. 识别新包和已有商品的重复关系。
2. 提取型号、颜色、尺码、鞋面、鞋底、闭合和图片证据。
3. 清除尺寸重复图、国内电商详情图和不合规文案。
4. 生成 `00_上架填写表.md`、6 张主图计划、颜色 SKU 图和英文详情页任务单。

### 阶段 2：读取现有线上商品

1. 拉取在售商品 ID、标题、状态、类目和质量分。
2. 拉取目标相似商品的完整字段、SKU、主图、详情、价格、包装和交期。
3. 对比新款与现有商品，决定新发、并入 SKU、延后或放弃。
4. 查询最近 7/30 天曝光、点击、CTR、询盘和商机，选择真正值得扩展的方向。

### 阶段 3：生成草稿

1. 让 Work Agent 解析素材包并预测类目。
2. 将本地填写表合并到平台草稿，不让工具自行猜高风险字段。
3. 执行发品预检。
4. 输出字段 diff：标题、关键词、属性、SKU、主图、详情、价格、MOQ、包装、交期。

### 阶段 4：人工确认

必须由用户确认：

- 发布商品和目标类目。
- 材质、尺码、颜色和 SKU。
- 价格、MOQ、样品、包装、交期和物流。
- 将创建草稿还是直接发布。

### 阶段 5：发布与回查

1. 优先发布草稿或单款商品，不做未经审阅的批量直发。
2. 发布后只读回查所有关键字段。
3. 7/14/30 天记录曝光、点击、询盘和买家问题。

## Accio Work 当前接入条件

当前状态（2026-08-10 BQ031 实测后更新）：`workctl v0.1.43` 已安装，Alibaba 账号已认证；已经通过 Accio Desktop 本地网关成功调用商品查询、草稿编辑、草稿提交和提交后回查接口。

2026-08-10 现场复核：

- Accio Desktop `0.28.5` 正在运行，账号已登录，当前工作区显示为“贝强”。
- “国际站生意助手”界面已确认具备：市场洞察、发品、客户接待、图片、视频、营销、物流、店铺装修。
- 历史任务中存在“阿里巴巴国际站店铺只读诊断”“全店29款商品优化总表建立”“国际站商品只读核验与选款”“A502与A503合规优化发布执行”等记录，说明该账号已实际使用过商品查询、诊断和发布链路。
- 单纯打开 Accio Desktop 不会把本地网关令牌自动注入已经运行的 Codex shell。`workctl doctor env` 显示桌面网关 `127.0.0.1:4097` 正常，但当前 shell 缺少 `ACCIO_GATEWAY_TOKEN` 和 `ACCIO_WORKSPACE_ROOT`。
- 不打印、不复制、不写入仓库，也不从进程环境导出网关密钥。若当前 shell 不是 Accio 启动的进程，可读取 Accio 自己生成的 `gateway-cli.json`，仅在同一个临时命令进程中把 `url` 的端口和 `password` 注入 `GATEWAY_PORT`、`ACCIO_GATEWAY_TOKEN`，命令结束即失效。本方法已在 BQ031 发品中验证，不得把密码值写入日志或文档。
- 后续再次核验时，`workctl auth status` 仍显示 Alibaba 账号已认证，但商品、访客、会话和 RFQ 的命令发现均因当前进程缺少 `ACCIO_GATEWAY_TOKEN` 返回 `desktop_not_attached`。这说明“Accio 已打开/账号已登录”与“当前 Codex 进程已接入经营工具”是两个独立状态。
- 网关未附着时，可使用已登录国际站后台完成只读核验；但后台页面读取只是兜底方式，不能代替 Accio 的批量结构化查询、草稿预检和异步任务管理。

要恢复完整能力：

1. 打开 Accio Desktop。
2. 优先从 Accio Desktop 启动/附着工作进程；若现有进程未附着，则使用 Accio 运行时配置做一次性环境注入，且禁止输出凭证值。
3. 重新执行只读命令发现，确认商品查询、商品详情、质量分、数据参谋和素材包发品命令的当前 schema。
4. 先完成只读拉取和一次 dry-run/预检。
5. 任何真实创建草稿、编辑或发布动作都要再次展示目标商品和字段 diff，并由用户明确确认。

## 三种使用方式比较

| 方式 | 自动化程度 | 风险 | 建议 |
| --- | ---: | ---: | --- |
| AI 生成填写表，人工后台录入 | 中 | 低 | 适合最初 1-3 款，便于建立正确模板 |
| AI + 浏览器辅助填写，发布前停下 | 中高 | 中 | Accio Work 不稳定时的备用方案 |
| Accio Work 素材包草稿 + 预检 + 人工确认发布 | 高 | 可控 | 推荐目标方案 |
| 未审阅批量直发 | 最高 | 高 | 不采用 |

## 接口优先路由（BQ031 实测）

1. 浏览器只做登录、页面研究和最终买家端核验，不再逐格录入或逐张上传。
2. 已存在草稿时，先用 `icbu.product.search` 或 `icbu.product.list-information` 读取草稿；不得重新生成第二个商品。
3. 用 `list-information` 分模块读取：基础信息、SKU/交易、履约、详情。数组参数优先通过标准输入传给 `--component-list '@-'`，避免 PowerShell 引号破坏 JSON。
4. 只修改明确缺失字段。BQ031 实测只补了 `productKeywords`，没有用不确定 URL 覆盖 SKU 颜色图。
5. 用户确认过发布范围后，用 `icbu.other.submit-draft --yes` 提交。返回 `submit draft success` 只代表提交成功，不等于已经在线。
6. 提交后必须区分三种状态：
   - `draft`：仍可编辑的草稿；
   - `copy`：已提交副本/审核中的版本；
   - `trunk`：正式线上正本。
7. `copy` 已生成且 50 个 SKU 获得正式 SKU ID、但 `trunk` 返回 `Record does not exist.` 时，记录为“已提交，待审核/生效”，不得写“已发布上线”。
8. `productDescType=5` 且 `isAiEdit=true` 表示 AI/HTML 富文本渲染链路。结构化 `detailImage/companyImage/faqs` 未返回，不等于前台没有详情内容；上线后仍须用买家公开页做第二证据源核验。

### BQ031 已验证接口链路

```text
health
→ schema（按当前目录发现命令）
→ search / list-information(draft)
→ 与 BQ030 list-information(trunk) 对照
→ product-edit-draft-basic-info（仅补关键词）
→ list-information(draft) 回读
→ submit-draft --yes
→ list-information(copy) 回查
→ trunk 上线后再次回查
```

### 常见失败与恢复

- `desktop_not_attached`：当前进程未继承 Accio 网关环境；确认 Accio 正在运行，再做一次性运行时注入。
- 网关健康检查 `HTTP 404`：误用了 `relayPort`；`GATEWAY_PORT` 应取运行时 `url` 的端口，本次为本地 HTTP 网关端口，而不是 relay 端口。
- `componentList expects a JSON array`：PowerShell 把数组 JSON 拆坏；改用管道输入 JSON 数组和 `--component-list '@-'`。
- `Record does not exist.`（trunk）：刚提交时通常尚无正本；查 `copy`，不要重复提交或重建商品。
- 结构化详情为空：先看 `productDescType`，富文本类型必须做前台二次核验。

## 下一次实际执行建议

BQ031 已完成第一款“本地素材 → 后台草稿 → 接口回读 → 缺项编辑 → 提交 → copy 回查”的标准样板。下一步等待 `trunk` 生效，完成买家公开页与质量分回查；随后把同一流程复制到下一款新品。R1601 的 Canvas/Stretch Fabric 冲突仍应单独修复，不与新品发布混做。

## Accio 标准任务提示词

### 任务 A：全店商品只读核验

```text
这是只读任务。查询当前阿里巴巴国际站店铺的全部已发布商品和草稿，输出：productId、型号、完整标题、叶子类目、产品分组、状态、质量等级/PIS、主图数量、视频、关键词/卖点完整度、核心属性、SKU、价格区间、MOQ、包装、交期、近30天曝光、点击、CTR、询盘。检测标题重复、关键词空缺、模板化字段和标题/属性/图片矛盾。重点核验 R1601 是否仍存在标题 Canvas 与 Upper Material=Stretch Fabric 的冲突。把商品分成：保留推广、优先优化、合并/暂停、证据不足四类。不要编辑商品、不要创建草稿、不要发布、不要发送消息。无数据字段明确写“未返回”，不得猜测。
```

### 任务 B：BQ031 素材包发品预检

```text
这是只读预检任务，不创建草稿、不发布。读取贝强工作区：01_产品资产/02_可发布素材/00_最终上传/BQ031_ZX2116，并以 00_上架填写表.md 和真实图片为事实来源。完成：与现有商品去重、类目候选、标题/关键词建议、必填属性清单、SKU颜色映射、主图与详情页合规检查、发品预检、缺失字段和风险列表。已知方向：ZX2116、宽头/大码38-47方向、五种颜色、针织鞋面、EVA软底证据、约187g/只的图片证据。材质、尺码表、MOQ、价格、样品、包装、交期、定制和贸易条款如未确认必须标记待确认，不得补猜。输出“可直接使用字段/必须确认字段/禁止使用字段/预检阻塞项”。
```

### 任务 C：RFQ 与客户开发只读方案

```text
这是客户开发只读任务。检索近12小时与 wide toe box walking shoes、comfort walking shoes、lightweight slip-on shoes、breathable knit casual shoes 相关的国际站 RFQ；结合贝强定位筛选美国和欧洲市场的进口商、批发商、Amazon/TikTok卖家、品牌/private-label买家和采购代理。输出 RFQ标识、国家、采购品类、数量、关键要求、发布时间、匹配产品方向、A/B/C优先级、推荐回复结构和仍需确认的报价数据。不得自动报价、不得发送消息、不得承诺FOB最终价格、MOQ、交期或证书。过滤零售消费者、医疗/矫形承诺和明显不匹配需求。
```

## 结果沉淀规则

每次 Accio 任务完成后，必须把结果保存到工作区，而不是只留在聊天记录：

```text
02_Alibaba运营/01_店铺与全店诊断/Accio全店只读核验_YYYY-MM-DD.md
02_Alibaba运营/02_单品优化记录/BQ031_Accio发品预检_YYYY-MM-DD.md
04_客户开发/01_线索与CRM/Accio_RFQ筛选_YYYY-MM-DD.md
```

每份记录必须包含：

1. 数据窗口和账号/店铺口径。
2. Accio 实际返回的事实与未返回字段。
3. 商品 ID、任务 ID 等本轮上游标识；不得跨轮猜测复用。
4. 成功步骤、失败原因和恢复入口。
5. 本次新知识、与旧结论冲突的地方、下一步动作。
6. 是否发生线上写入；没有写入时明确标注“只读”。

后续更新长期规则时，优先修改本 SOP 和对应的单品/全店记录，避免新增多个互相冲突的零散文档。
