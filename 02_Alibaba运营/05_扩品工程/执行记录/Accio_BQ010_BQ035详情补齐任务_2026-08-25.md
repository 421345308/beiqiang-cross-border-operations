# Accio BQ010 / BQ035 结构化详情补齐任务

> 状态：已准备，尚未向 Accio 提交线上写操作。

调用“国际站商品信息优化”的存量治理能力和“商品发布”的编辑模式。只补齐以下两款当前缺失的结构化详情，不修改标题、型号、主图、SKU、价格、MOQ、交期、包装及其他交易字段：

- BQ010 / R1601 / productId `1601825160204`：严格使用 `BQ010_结构化详情修复载荷_2026-08-25.json`。
- BQ035 / A001 / productId `10000046705043`：严格使用 `BQ035_结构化详情修复载荷_2026-08-25.json`。

每款写入 4 张 `detailImage` 和 5 张 `companyImage`，地址必须原样使用载荷中的正式 `https://sc04.alicdn.com/` URL，不得替换为 Accio 临时地址或嵌套地址；公司介绍原样使用 `companyDesc`。

发布前检查：

1. `productId + sourceModel` 与在线正本一致。
2. 4 张产品图与当前款一致，5 张公司图均为贝强中性公司图。
3. 所有 URL 均为直接 `sc04` 地址，坏地址为 0。
4. 不包含 Flyknit、医疗功效、固定样品费退款/抵扣、未经证实宽楦或其他额外承诺。
5. 除结构化详情外的全部字段保持不变。

发布后回读 copy、trunk 和公开页，确认每款 4 张产品详情图、5 张公司图均存在且顺序正确，并返回逐款结果和公开链接。
