# HR018-A/B/C 质量分诊断与修复门槛

检查日期：2026-09-23。使用已授权的 `alibaba.icbu.product.score.get` 对原商品 ID 逐条实时读取。阿里[官方接口文档](https://developer.alibaba.com/docs/api.htm?apiId=47461)确认该接口返回商品质量分；以下 `problem_map` 是本店当次接口响应中的诊断字段，不把分数当作货源或可售证明。

| 链接 | Product ID | 当前分数 | 诊断 | request_id |
| --- | --- | ---: | --- | --- |
| HR018-A | `1601943561380` | `3.90` | `extendProblemMap.inventory=true`；其余返回诊断项均 `false`，`errorReasonList=[]` | `0bab02fe17901338788843607` |
| HR018-B | `1601943555428` | `3.90` | 同上 | `0bab02fc17901339127671383` |
| HR018-C | `1601943607120` | `3.90` | 同上 | `210829b717901339263316122` |

三条响应还返回 `boutique_tag=1`，但 `final_score=3.90`，不能把标签解释成用户所要求的质量分已合格。标题、图片数量/质量、类目、价格、SKU 图文、卖点冲突等当次诊断均未报错。前轮已将每条 30 个未经核实的 `999` 库存校正为 `0` 并独立回读；结合本次唯一 `inventory=true`，**库存是当前质量分低的直接诊断项**。这不是通过补标题或堆图能安全消除的扣分。

同日再次通过官方接口读取 A/B/C：分数仍均为 `3.90`，唯一问题仍为 `inventory=true`；本次 request_id 分别为 `2105584317901365277077032`、`213c932b17901365438646111`、`0b51802d17901365458903600`。进一步核对[第四轮公开货源候选](./HR018_分层网面越野风鞋初筛_2026-09-23.md)：9029、8200、R1207 与旧鞋外观不对应，HW3008 已下架；因此没有合法依据把某一候选的公开库存数量填到旧 HR018 SKU 上。

## 历史诊断路径（已由下方负责人新规则覆盖，勿继续按此阻断）

1. HR018 仍是概念鞋，没有通过实物/采购渠道核验；当前 `HOLD_SOURCE`。不得为抬分恢复 `999`、填任意正库存或把库存留空表示“无限库存”，也不得据旧页面承诺发货。
2. 先为同一 HR018 家族选定一款能执行采购、无第三方标识争议且实拍素材完整的真实鞋；记录来源、货号、页面色码、进货价及联系/下单入口。若与旧概念鞋不同，A/B/C 必须统一整体换款，旧图文/型号/SKU 不保留。逐码精确库存与订单交期不再是定款前置，接单时复核。
3. 最低美元销售价按实际进货价 ×1.30、当次汇率与费用重新计算；完成真实产品的六主图、至少四张专属详情、五公司图、视频适配及 SKU 绑定后提交。按负责人约定在新鞋 SKU 上统一设置 `999` 可询货标记，再分别回读商品、库存、质量分和买家公开页，不把 `999` 解释为实物数量。
4. 质量验收条件：三条 `final_score` 达到平台当前合格要求且 `inventory=false`，同时来源/价格/图文/公开页门禁全部通过。买家下单前仍须核实本单可供，不宣称已备有 999 双。

本段为当时诊断，不是现行阻断结论；目前已有 T001 采购入口，仍未完成线上换款和质量复测。

## 2026-09-23 负责人新规则与后续进展

上述“必须找到完全同款、按精确可交付数量填库存”的限制已被负责人新要求取代，不再作为 HR 改造阻断项。可选择相似但真实可采购的鞋款，前提是 A/B/C 整组换成同一货号，旧概念鞋的图文、色码与 SKU 全部同步替换；已映射且页面鞋样一致后统一以 `999` 作可询货标记，而非实物库存数。订单到来再按当日供应商回复核对数量、价格和交期。

已选[致盛 T001](https://zhisheng.sooxie.com/detail/2486119)为 HR018 的优先采购入口；其页面 ¥48、黑/灰/米 EU39–44、逐码标“充足”，详见[整组替换方案](./HR018_T001整组替换方案_2026-09-23.md)。当前从“无采购入口”转为 `SOURCE_ROUTE_FOUND / REPLACEMENT_PREP`，并非已经改好线上页面。三条原页仍是旧概念鞋、质量分仍为 3.90；完成实拍图文、18 SKU、≥30% 价格及 `999` 标记后，须用官方接口重新回读商品、库存、质量分并验收公开页。旧页不能据 T001 直接接单发货。
