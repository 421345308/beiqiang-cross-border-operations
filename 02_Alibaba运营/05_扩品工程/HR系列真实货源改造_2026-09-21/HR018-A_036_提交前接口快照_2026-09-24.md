# HR018-A / 036：提交前官方接口快照

2026-09-24 官方 `alibaba.icbu.product.get` 回读商品 ID `1601943561380`（request_id `213f5a2117901860324697930`）：`status=approved`、`display=Y`、类目 `201334413`，标题仍为 `OEM Sand Beige Mesh Breathable Lace-up Light Weight Walking Shoes Chunky Sole Trail Casual Sneakers EU 36-45 Model HR018-A`，六张主图及 30 个旧 SKU 均属于旧概念鞋。**因此本轮的图片入库不是线上换款或质量修复。**

官方 `product.schema.render` 对同一商品回读 `biz_success=true`（request_id `2105584317901860651686770`），当前 XML 仍有 `productTitle`、`icbuCatProp`、`saleProp`、`sku`、`scImages`、`imageVideo/detailVideo`、`productKeywords`、`ladderPrice`、`pkgMeasure/pkgWeight`、`productDescType`、`detailImage/companyImage`、`textDesc/companyDesc` 等字段。**字段出现在读取 XML 不代表 API 支持写结构化图库**；四产品详情和五公司图仍按当前卖家后台/Workctl 可支持的结构化编辑路径执行，不能凭一次 Schema 成功返回就报完成。

本款已具备[六主图及双色 SKU](./HR018_036_询价包装主图入库回执_2026-09-24.md)、[四产品详情图](./HR018_036_四张产品详情图入库回执_2026-09-24.md)和[五公司图候选](./HR018_036_公司图适用性与正式地址_2026-09-24.md)的本店图片银行地址。来源[路崎 036](https://luqi.sooxie.com/detail/2439422)公开页标价 ¥72，黑/米 EU39–48；最低销售价须不低于核验价 ×1.30（当前基线 ¥93.60 等值），美元梯度提交前复算。来源逐层材料与本类目必填选项已[单独核过](./HR018_2026-09-24在售替代货源核验.md)，不得把旧图的宽楦、防滑/医疗词、旧蓝色或旧 EU36–45 带入。

唯一下一动作：先准备 HR018-A 的完整 036 草稿——新标题/型号、黑/米 × EU39–48 的 20 SKU、两张颜色图、六主图、四详情图、五公司图及明确“外部采购款”的公司导语、按当日价格及汇率核实的报价；检查视频是否仅为通用工厂证明、当前类目必填属性和逐项地址后提交。审核生效、页面与来源身份一致之后，再以库存专用 API 将有效 SKU 写为 `999` 可询货标记并回读，复测质量分与公开页，最后同货号处理 B/C。

## 2026-09-24 再核对：不靠旧 SKU 数字修分

官方 `product.get` request_id `0b51802d17901867304301732` 再次确认正式版仍为旧沙色概念鞋、旧图、30 个旧 SKU 均为 `0`，`approved/Y`。卖家编辑页质量助手显示 `3.9`，库存项提示“部分规格的可售数量小于最小起订量”，并另报旧自定义属性不被当前类目支持。界面中曾试开“删除全部图片”以探查正式图片银行选择路径，**未保存、未提交**；关闭编辑页后以上述官方回读确认线上仍旧。当前 Workctl 命令不在本机 PATH，公开 npm 包未找到，内网仓库查询无响应；不能把这些尝试写成草稿已保存或换款完成。继续使用官方 OpenAPI 处理其有明确写入契约的字段，结构化详情通过卖家编辑器完成；图片银行内嵌选择器若持续无法可靠选取，不先提交残缺商品。根据负责人最新统一口径，`999` 仅在 036 鞋样、色码、图文与有效采购入口完成绑定后写入**新**的 20 个 SKU，不给旧 30 个概念 SKU 恢复可询货状态，也不追问逐码实时库存。
