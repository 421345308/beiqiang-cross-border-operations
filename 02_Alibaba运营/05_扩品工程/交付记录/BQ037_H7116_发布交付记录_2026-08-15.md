# BQ037 / H7116 发布交付记录

更新日期：2026-08-15

## 结果

- 唯一商品 ID：`1601924347721`
- 操作：仅发布该已有草稿；未调用新建商品能力，未生成第二条 BQ037。
- 当前状态：平台审核中。发布后草稿已消费，在线总数为 36，BQ037/H7116 在线匹配暂为 0；审核通过后预期总数为 37。
- BQ032-BQ036：未因本次 BQ037 发布被复制或修改；其中 BQ036 已审核上线。

## 发布前回读与修复

1. `workctl icbu product search --productId 1601924347721`：网页端 `Failed to fetch` 没有改变底层草稿，标题、型号、主图和商业字段仍为确认版。
2. 草稿基础属性编辑：保存 `Midsole=EVA`（valueId `3376399`）、`Lining=Mesh`（`6821988`）和 `Plush`（`4141322`）。
3. 保留事实：
   - Title: `Wholesale Knit Lace-Up Casual Shoes Rubber Toe Cap EVA Sole Walking Sneakers with Fleece Option`
   - Model: `BQ037 / H7116`
   - Upper: `Knitted Textile Upper`
   - Outsole/Midsole: `EVA`
   - Closure: `Lace-up`
   - Toe: `Round Toe` + `Rubber Toe Cap`
4. 未增加未确认的 Feature、Season、Pattern、证书或性能宣称。

## 图片与 SKU 门禁

- 主图：6 张。
- 详情图：6 张；详情编辑结构固定为 `operationType=ADD + newImageUrl + imageIndex`。
- 颜色图：7 张。
- SKU：72 个有效组合；除 Purple 仅 EU35-40 外，其余 6 个颜色为 EU35-45。
- `Black/White Fleece` 与普通黑白鞋共用外观图，仅表示里衬选项，不把该图作为加绒实拍证据。
- `list-information` 当前持续报 CLI 内部错误，72 SKU、7 颜色图、6 详情图及关键词在发布后不可直接回读。现有证据为素材组装成功、草稿字段编辑成功及发布预检通过；审核上线后必须在 trunk/买家页复验。

## 正式提交回执

- 命令：`workctl icbu product publish-draft --productId 1601924347721 --yes`
- 回执：`success:true, data=1601924347721`
- 后续只读回查：草稿 `search` 返回空，说明同一草稿已消费；`list-id` 目前为空，说明尚未成为在线正本。
- HS：平台自动归类为 `64041937`，当前能力没有编辑入口；不要把原计划值写成已生效。

## 关联异常：BQ033 重复草稿

- 主商品 `1601924345456`：在线，保留，未操作。
- 重复项 `1601924438053`：`search` 有草稿数据、`list-id` 为空，确认是未上线、非审核草稿。
- 用户已确认删除该精确草稿；但 Workctl/Accio 当前无草稿删除或下架命令，商品管理域名又返回 `ERR_CONNECTION_CLOSED`，因此未冒险绕过限制。
- 待后台恢复后，仅在商品管理草稿列表删除 `1601924438053`，并再次回读两个 ID；不得操作主商品 `1601924345456`。

## 下一步

1. 复查 BQ037 审核结果；上线后核验总数 36→37、标题、6 主图、价格、MOQ、关键属性、72 SKU、7 颜色图、6 详情图和质量分。
2. 后台恢复后删除 BQ033 重复草稿 `1601924438053`，确认主商品仍在线。
3. 继续 BQ038 / M889：先查重、建立唯一草稿、回读纠偏、发布预检，再提交。
