# 国际站合规扩品工程总控

更新日期：2026-08-16

本文件是扩品工程的唯一续跑入口。每次继续任务时，先读本文件、`数据/扩品批次台账.csv`和`数据/国际站预上架商品总表_2026-08-14.csv`，不重新下载和重做候选审计。

## 一、当前结果

| 项目 | 结果 |
| --- | --- |
| 国际站已有本地正式产品库 | BQ001-BQ031，31 款 |
| 搜鞋网贝强工厂店公开商品 | 72 款 |
| 候选缺口 | 48 款，已全量下载 |
| 下载图片 | 1,073 张，48 包全部通过数量校验 |
| 独立安全款 | 20 款，已编为 BQ032-BQ051 |
| 重复/合并 | 10 款，不新建链接 |
| IP/混包隔离 | 18 款，不发布 |
| 预上架资产 | 20 套填写表 + 120 张主图 + 120 张详情图 + 80 张颜色 SKU 图 |
| 当前主状态 | BQ032-BQ039 在线（在线总数 39）；BQ040 已提交审核（草稿 1601924792152 已消费）；BQ041-BQ051 为 `ASSETS_READY`；BQ033 误触发重复草稿 `1601924438053` 保留在草稿箱（用户指示不删除，主商品不受影响） |

本地交付根目录：`02_可上传素材/00_最终上传/BQ032_ZX2212` 至 `BQ051_K6689`。

## 二、本批确认边界

### 产品事实

- BQ032-BQ051 全部使用 `Regular Fit / 常规楝型`；不对外使用 Wide Toe Box、Wide Fit、Orthopedic、Medical、Diabetic、Bunion Friendly 或 Barefoot。
- 材料、尺码、颜色以每个来源数据包为准，不从 BQ030/BQ031 继承产品材料。
- 闭合方式以实物图为准；ZX2212、H7116、M889、206、8008、811、A230、A828 的国内标题与图片有冲突，已按图片纠正为系带款。
- A212-1 不单独发布，已作为 A212 的加绒 SKU 合并到 BQ048。

### 商业字段

负责人确认本批沿用已有成功产品模板：

- MOQ: 2 pairs
- 2-49 / 50-99 / 100+ pairs: USD 9.49 / 9.19 / 9.09 per pair
- Lead time: 31 days for 100 pairs
- Package reference: 34×23×13 cm / 0.5 kg / 1 pair
- General cargo
- Standard shoe box or plastic bag
- Logo、insole、color 和 packaging 可根据数量和最终要求讨论
- 样品可安排；样品费和快递费下单前单独确认

## 三、素材生产方法

可重复执行：

```powershell
python 08_工具链/01_业务脚本/国际站扩品/build_sooxie_prelisting_batch.py --workspace C:\Users\spq\Desktop\贝强
```

配置正本：`08_工具链/01_业务脚本/国际站扩品/sooxie_prelisting_profiles_2026-08-14.json`。

处理规则：

1. 只对真实源图做选图、裁切、留白、尺寸归一和英文信息排版。
2. 不使用 AI 生成或改变鞋型、颜色、外底、Logo 和结构。
3. 国内中文详情图只用作材料/结构证据，不直接上传。
4. 主图 1000×1000，详情图 1200×1200，颜色图 1000×1000。
5. 每款自动验收6张主图、6张详情图、颜色名/颜色图数量一致和尺寸正确。
6. 视觉总览在 `02_可上传素材/00_最终上传/99_批次视觉验收_2026-08-14/`。

## 四、后台执行链路

Accio Desktop + Workctl 已在 BQ031 上证实可用，届时以当前动态 schema 为准，不照搬旧命令。

```text
用户登录国际站/Accio
→ 当前 schema 和健康检查
→ 只读回查在线、草稿、拒审和违规列表
→ 批量上传本地图到图片银行
→ 记录本地文件→阿里 URL 映射
→ 按填写表建立唯一草稿
→ 回读标题/关键词/属性/图片/SKU/价格/MOQ/交期/包装/详情/物流
→ 通过后提交
→ copy/trunk/买家页核验
→ 7/14/30 天数据复盘
```

执行节奏：先用 BQ032 跑通一个完整草稿并回读；通过后每批 3-5 款顺序建立，不并发提交 20 款而失去错误定位能力。素材和字段已全部就绪，分批只是后台验收策略。

## 五、链接布局与规则

1. 上齐真实独立鞋型，用不同结构、人群、闭合和尺码段扩大搜索覆盖。
2. 不把同一鞋仅换标题、主图、详情页和关键词后重复发布。
3. 不用“违规后还有备用链接”作为账号安全策略。违规必须修复根因，IP 风险款不上架。
4. 同款不同颜色和加绒变体优先做同链接 SKU，不分散权重。
5. 发布后以曝光、点击、CTR、询盘数和询盘质量决定优化，不因零曝光继续制造重复链接。

## 六、下一动作

国际站后台当前已登录，BQ032-BQ035 已跑通浏览器或 Accio/Workctl 发品链路：

1. BQ032：商品 ID `10000046653813`，质量分 5.0，状态 `ONLINE`，月曝光 0。
2. BQ033：主商品 ID `1601924345456` 已审核上线并保留；旧排队指令形成的重复项 `1601924438053` 已回读为草稿态，不在线、非审核。用户已确认删除，但当前 Workctl 无草稿删除/下架能力，商品管理域名又返回 `ERR_CONNECTION_CLOSED`，待后台恢复后只删除该精确 ID。
3. BQ034：商品 ID `11000037600317`，质量分 5.0，已提交并核对为“审核中”。
4. BQ035：商品 ID `10000046705043`，质量分 5.0，已提交并核对为“审核中”。
5. BQ036：商品 ID `1601924292931` 已审核上线，在线总数由 35→36；继续回读质量分、标题、主图、价格、MOQ、关键属性和 22 个 SKU。
6. BQ037：只对原草稿 ID `1601924347721` 保存中底 EVA、里衬 Mesh + Plush，`publish-draft` 预检通过并返回同一 ID；草稿已消费，当前在审核队列，未新建第二条。审核后复验在线总数 36→37、质量分和图片/SKU。
7. 从 BQ038 开始；每款先建立唯一草稿并回读，纠正平台 AI 擅自补写的材料/功能字段，并以发布预检验证主图、颜色/SKU 和详情图不为空后再提交。
8. BQ032、BQ033 主商品、BQ036 进入 7/14/30 天数据观察；BQ034、BQ035、BQ037 待审核通过后再开始观察，不因初始零曝光复制重复链接。
9. 每款写回 `alibaba_product_id`、提交/在线状态和图片 URL 映射。

## 七、后台实测沉淀（2026-08-15）

### 1. 图片与规格

- 上传前把本地文件统一加上 SKU 前缀，例如 `BQ033_01_main.jpg`。图片银行中通用文件名会产生错款风险。
- 图片银行可能按相同内容去重并保留另一个文件名；选择时以画面内容和 SKU 前缀双重核对。
- 国内数据包每张图必须在上传前人工可视化检查：中文促销字、国内节日文案、乱码英文、第三方标识、买家评论和未确认功能图不得直接上传。
- 不得为了去字让 AI 重画鞋型、编织纹理或外底；若风险文字在边缘，优先做确定性裁切并再回读画面。BQ035 主图 03 即采用裁切去除无关乱码英文，且未改变产品结构。
- 颜色值必须从平台下拉中确认并按 Enter 落值；仅输入或失焦可能在保存后回退。
- 颜色、标题、型号、主图顺序和 SKU 图完成后必须“保存 → 重载 → 回读”，不能只看当次页面。

### 2. 页面遮挡

- 右侧 AI 助手抽屉和客服浮窗会透明覆盖规格图片或删除按钮，表现为按钮可见但点击无效。
- 操作前关闭 AI 抽屉；客服浮窗遮挡时先拖离目标区域，再执行图片银行或删除操作。

### 3. 结构化详情

- 结构化详情采用懒加载。重载后空容器不等于详情为空，必须先滚动到“商品详情信息”区域再判断。
- 复制旧款只允许复用版式。必须逐项替换产品亮点、型号、材料、尺码、颜色和产品图片，并扫描旧 SKU 残留。
- 本批标准产品详情为 6 张：产品概览、规格、尺码、颜色、订单支持、OEM 询盘；无新款素材的旧场景图模块直接删除。
- 编辑器保存顺序：`保存 → 编辑完成 → 确认同步 App → 主发布页保存 → 重载回读`。
- 最终门禁：详情预览中旧 SKU 命中数为 0、产品图为 6 张、平台质量分 5.0，才允许提交。

### 4. 提交与续跑

- 提交成功页的 `primaryId` 是商品 ID，以成功页和“审核中”状态作为提交完成凭据。
- 禁止把现有商品 URL 手工改成 `pubAction=similarPost`。实测该方式会复用源商品 ID，甚至在未点击“保存”时覆盖源商品内容。
- 浏览器复制只能从平台 UI 进入 `发布新商品 → 发布类似 → 选择源商品`；真正的新建链接 URL 使用 `opType=similarPost`。新草稿仍需检查是否获得独立 ID，提交后必须再用商品总数和商品列表回读证明没有覆盖旧链接。
- Accio/Workctl 优先采用 `最终素材 → CDN URL → materialInfo → infer/category schema → 草稿 → search 回读 → 属性纠偏 → publish-draft → 审核队列`。最终图片已制作完成时使用 `decorationPlan=none`，禁止 AI 重画或自动改写商品事实。
- 平台 AI 解析结果不能直接发布。BQ033 实测出现标题改写、Outsole=EVA、Lining=Mesh、Anti-Slip 和颜色 SKU 错配；必须以填写表和实物图为准回读并删除未确认字段。
- Workctl 草稿属性编辑采用 `attrNameId + attrValueId + attrValue + operationType`；已有属性用 `EDIT`，新增属性用 `ADD`。接口返回 success 后仍必须 search 回读，不能把成功回执当作字段已生效。
- 素材生成脚本重跑不得覆盖已上架/已提交/草稿待提交状态；脚本已改为保留超过 `ASSETS_READY` 的运营状态、下一动作和更新日期。
- Accio 重启、升级、任务切换或存在旧消息排队后，必须先只读查询目标 ID 的草稿/审核/在线状态及同型号匹配数，确认旧任务终止后再写入。BQ033 的重复审核商品即由旧指令延迟执行造成。
- `publish-draft` 发布预检是详情图真实性门禁。BQ036 首次预检返回 `detailImage:详情图片不能为空`；正确修复结构为 `operationType=ADD + newImageUrl + imageIndex`，旧结构 `[{imageUrl:...}]` 会出现接口成功但实际未落值。
- BQ037 闭环后从 BQ038 开始；不要重新创建 BQ032-BQ037，也不要从未核验的旧款复制产品字段。

### 5. BQ033 图片与发布验收（2026-08-15）

- 本地 6 张主图、6 张详情图、4 张颜色图已逐张目视检查：同一鞋型，无中文、国内平台水印、第三方标识或旧款残留；Black / Grey / Off White / Pink 对应正确。
- 草稿回读最终值：标题为确认版；Upper Material=`Knitted Textile`；Outsole/Midsole/Lining=`Other`；Closure=`Lace-up`；Feature 仅保留 `Breathable`、`Lightweight`；未写 EVA 或 Anti-Slip。
- `publish-draft` 返回成功，草稿已消费，商品 ID `1601924345456` 进入审核队列。审核完成前不得把“在线总数 34→35”或“质量分 5.0”写成已完成事实。

### 6. BQ036 详情图门禁与发布验收（2026-08-15）

- 本地 6 张主图、6 张详情图、2 张颜色图已逐张目视检查：同一鞋型，无中文、国内平台水印或第三方标识；Black/White 与 All Black 对应正确。
- 唯一草稿 ID `1601924292931`；标题、型号、Upper、EVA outsole、Slip-On、Round Toe、价格、MOQ、交期和包装已回读，平台 AI 猜测的 Feature/Season/Lining/Midsole/Pattern 已删除。
- 首次正式发布被预检拦截，原因是草稿详情图实际为空；随后使用 `operationType=ADD + newImageUrl + imageIndex` 补齐 6 张详情图并重新预检通过。
- `publish-draft --productId 1601924292931` 返回 success，原草稿已消费并进入审核队列；当时在线总数仍为 35、BQ036 在线匹配为 0，需审核后复验总数 35→36、标题/主图/价格/MOQ/关键属性和质量分。
- SKU/详情组件读取接口仍存在 `list-information` 内部错误；22 个颜色×尺码 SKU、6 详情图、2 颜色图在审核通过后继续回读，不把接口不可读写成已验证。

### 7. BQ037 混合里衬与 HS 门禁（2026-08-15）

- 本地 6 张主图、6 张详情图、7 张颜色图已逐张复核；已用来源包白底实拍替换带 `Monday` 袜子文字和国内场景的图片，不使用 AI 重画鞋型。
- 唯一草稿 ID `1601924347721`；标题固定为 `Wholesale Knit Lace-Up Casual Shoes Rubber Toe Cap EVA Sole Walking Sneakers with Fleece Option`，型号为 `BQ037 / H7116`。
- SKU 结构为 72 个：6 个常规颜色按 EU35-45，Purple 仅 EU35-40；`Black/White Fleece` 与常规黑白共用外观图，只表示里衬差异，不把外观图写成加绒证据。
- 平台 AI 曾把整款写成 `Lining=Fleece` 并补写 `Midsole=none`；已删除这些错误值。网页依据来源包与 SKU 事实补齐必填材料为 `Outsole=EVA`、`Midsole=EVA`、`Lining=Mesh + Plush`，必填属性由 2/4 变为 4/4。
- “美国 HS 编码维护”先受必填属性门禁控制；当当前链接按“商机品”提交时，该模块不构成发布门槛。不得为了打开模块盲填完整美国 HS 编码，若转交易品再由货代/报关行复核。

### 8. BQ037 发布与 BQ033 重复草稿处置（2026-08-15）

- 网页端一次保存/提交因 `Failed to fetch` 报颜色、商品图片和详情图片为空；随后用 `search --productId 1601924347721` 回读，确认底层草稿未被网页缓存错误改坏。
- 只编辑原草稿：`Midsole=EVA`（valueId `3376399`），`Lining=Mesh`（`6821988`）+ `Plush`（`4141322`）；标题、型号、鞋面、大底、闭合、鞋头和商业字段保持确认版，未增加 Feature、Season、Pattern 或证书。
- `publish-draft --productId 1601924347721 --yes` 返回 `success:true, data=1601924347721`；发布预检通过证明平台已接受完整主图/颜色/SKU/详情结构。发布后 `search` 返回空，说明原草稿已消费；`list-id` 暂无在线正本，故状态为审核中。
- 当次在线总数为 36（已含 BQ036），BQ037/H7116 在线匹配 0；审核通过后应变为 37。未调用 `publish-product`，未生成第二个 BQ037，也未改动 BQ032-BQ036。
- `list-information` 仍有 CLI 内部错误，72 SKU、7 颜色图、6 详情图和关键词无法在发布后直接回读；保留素材组装成功、字段编辑成功和 `publish-draft` 预检通过三段证据，审核上线后再做 trunk/买家页复验。
- BQ033 主商品 `1601924345456` 经 `list-id`/`list-score` 确认在线；重复项 `1601924438053` 经 `search` 有草稿数据、`list-id` 为空，确认是未上线、非审核草稿。此前曾计划后台恢复后删除，但用户现已明确要求保留，因此不再将其列为待删除项，主商品不动。

### 9. BQ038 唯一草稿创建、回读纠偏与发布（2026-08-16）

- 只读查重：list-name "BQ038" / list-name "M889" / list 在线匹配均为 0；发布前无同款。
- 素材：BQ038_M889 本地 6 主图（首图为 03_颜色图\01_black_white.jpg 白底搜索主图，后接 01_主图\01_main/02_upper/03_structure/04_sole/05_colors；未用较弱的 06_scene.jpg）+ 6 详情图 + 5 颜色图（Black/White、White、All Black、Black/White Fleece、All Black Fleece），共 17 张上传 upload-file 至 OSS 得到 CDN URL；decorationPlan=none，无 AI 生图。
- 链路：material-extract → material-collect --mode analysis → material-analysis（首次解析，AI 仅生成颜色级 5 SKU、主图 5 张、详情 7 张，且 productImages 含 3 条 ai_planned AI 计划图）→ 构造 merge_patch.json（v2 ops）全量修正：主图 6 张指定顺序、详情 6 张、55 SKU（5色×EU35-45 共11码）、16 条属性、3 关键词、清空 AI 计划图 productImages → material-collect --mode patch → material-analysis 合并（新 category_key `15f597d605a4b1688f50550d4adb4a0`）。
- publish-product --publishType draft（taskId=239342668）终态 status=2 全 success，materialPublish_saveDraft 返回唯一草稿 1601924732369。
- search 回读发现平台 AI 改写/新增：标题被改、Season=All Seasons、Style=Casual、Logo/Graphic Design=Custom Logo、Feature×4（Lace-Up/Chunky Sole/EVA Cushion/Fleece Option）、Midsole=none。经 product-edit-draft-basic-info 两轮纠偏：标题改回确认版、删除 Season/Style/Feature×4、Midsole none→EVA（valueId 3376399）、ADD Closure Type=Lace-up（80806811）；Lining=Mesh+Plush、Outsole=EVA、Toe=Round Toe、Upper、Model=BQ038 / M889、Origin 均正确保留。
- Logo/Graphic Design=Custom Logo 与 Packaging=Standard Shoe Box 为服务/包装模块属性（源自填写表 Customization/Packing Method，非虚构性能宣称），product-edit-draft-basic-info 三种 DELETE 方式均无法移除，记录为接口限制。
- 详情图补写：list-information 显示 detail 组件无 detailImage → product-edit-draft-detail 用 operationType=ADD + newImageUrl + imageIndex 写入 6 张，回读确认 6 张已落值（productDescType=4）。
- 首次 publish-draft 预检被关键词非法字符拦截（productKeywords 用了逗号分隔）→ 改为空格分隔后重试。
- publish-draft --productId 1601924732369 --yes 返回 success:true, data=1601924732369，预检通过即发布；发布后 search 返回空（草稿已消费）、list-id 为空（审核队列中）。
- 当次在线总数仍为 37（已含 BQ032-BQ037），BQ038/M889 在线匹配 0；审核通过后应变为 38。未调用 publish-product 直发，未生成第二个 BQ038，未改动 BQ032-BQ037。
- 接口限制：list-information 的 sku 组件返回空（55 SKU、5 颜色图无法直接回读），SKU/颜色完整性由素材组装成功 + publish-draft 预检通过间接证明；list-name 与 list-score 均为在线侧检索，草稿/审核态不可见。
- BQ033 重复草稿 1601924438053 本轮确认仍为草稿态（search 有数据、list-id 空），按用户指示保留，未删除、未操作。

### 9.1 BQ038 审核上线（2026-08-16 复核）

- 当日 BQ039 发布后回查在线总数时确认：list-score 全量 38 款，已含 BQ038 1601924732369（当日早间提交，审核已完成并上线，在线总数 37→38）。

### 10. BQ039 唯一草稿创建、回读纠偏与发布（2026-08-16）

- 只读查重：list-name "BQ039" / list-name "155" / list-name "BQ039 / 155" / list-name "Segmented Sole Casual Shoes" 在线匹配均为 0；发布前无同款（list-name "High Top Knit Sock" 模糊命中 BQ036，标题不同，非同款）。
- 素材：BQ039_155 本地 6 主图（首图为 01_主图\00_main_clean.png；该图由 AI 仅移除原图小字，未改变鞋型、颜色和结构；后接 02_upper/03_structure/04_sole/05_colors/06_scene；未用原 01_main.jpg）+ 6 详情图 + 2 颜色图（Black、Red），共 14 张上传 upload-file 至 OSS；decorationPlan=none，未生成虚构商品图。
- 链路：material-extract → material-collect --mode analysis → material-analysis（首次解析，AI 仅生成颜色级 2 SKU、主图 5 张、详情 9 张，productImages 含 4 条 ai_planned AI 计划图）→ merge_patch.json（v2 ops）全量修正：6 主图指定顺序、6 详情、22 SKU（Black/Red × EU35-45）、14 条属性（Sole Material=Segmented Foamed Sole，不推断 EVA/MD/橡胶配方）、3 关键词、清空 AI 计划图 → material-collect --mode patch → material-analysis 合并（新 category_key `a37ead29fc2f477a8441bbbc637e500f`）。
- publish-product --publishType draft（taskId=239342690）终态 status=2 全 success，saveDraft 返回唯一草稿 1601924755368。
- search 回读发现平台 AI 改写/新增：标题被改、Style=Casual、Lining=none、Midsole=none、Feature×2（Slip-On/Segmented Sole）。经 product-edit-draft-basic-info 一轮纠偏：标题改回确认版；删除 Style/Lining=none/Midsole=none/Feature×2；Lining 与 Midsole 补系统值 Other（valueId 4，不虚构材料）；ADD Closure Type=Slip-On（valueId 13325270）；Outsole Material=Segmented Foamed Sole（自定义值）与 Toe/Upper/Model/Origin 正确保留。关键词直接用空格分隔（吸取 BQ038 逗号拦截教训）。
- 详情图补写：list-information 显示 detail 组件无 detailImage → product-edit-draft-detail 用 operationType=ADD + newImageUrl + imageIndex 写入 6 张，回读确认落值（productDescType=4）。
- publish-draft --productId 1601924755368 --yes 返回 success:true, data=1601924755368，预检一次通过即发布；发布后 search 返回空（草稿已消费）、list-id 为空（审核队列中）。
- 当次在线总数 38（已含审核上线的 BQ038），BQ039/155 在线匹配 0；审核通过后应变为 39。未调用 publish-product 直发，未生成第二个 BQ039，未改动 BQ032-BQ038。
- 接口限制：list-information 的 sku 组件返回空（22 SKU、2 颜色图无法直接回读），完整性由素材组装成功 + publish-draft 预检通过间接证明；Packaging=Standard Shoe Box 为服务模块属性（源自填写表 Packing Method），API 无法删除，非虚构宣称。
- BQ033 重复草稿 1601924438053 仍为草稿态，按用户指示保留，未操作。

### 10.1 BQ039 审核上线（2026-08-16 复核）

- 当日 BQ040 发布后回查在线总数时确认：list-score 全量 39 款，已含 BQ039 1601924755368（在线总数 38→39）。

### 11. BQ040 唯一草稿创建、回读纠偏与发布（2026-08-16）

- 只读查重：list-name "BQ040" / list-name "1689" / list-name "BQ040 / 1689" / list-name "MD Foam Segmented Sole" 在线匹配均为 0；发布前无同款。
- 素材：BQ040_1689 本地 6 主图（首图为 01_主图\00_main_clean.png 干净白底首图，后接 02_upper/03_structure/04_sole/05_colors/06_scene；未用原 01_main.jpg。00_main_clean.png 为基于真实原图生成的干净搜索首图，不作材质/性能证据）+ 6 详情图 + 3 颜色图（Black/White、Grey、All Black），共 15 张上传 upload-file 至 OSS；decorationPlan=none，无 AI 生图。
- 链路：material-extract → material-collect --mode analysis → material-analysis（首次解析，AI 仅生成颜色级 3 SKU、主图 5 张、详情 7 张，productImages 含 3 条 ai_planned AI 计划图）→ merge_patch.json（v2 ops）全量修正：6 主图指定顺序、6 详情、21 SKU（Black/White、Grey、All Black × EU39-45）、14 条属性（Sole Material=MD Foam Sole，不推断其它配方）、3 关键词、清空 AI 计划图 → material-collect --mode patch → material-analysis 合并（新 category_key `b7bc623dfa39400389789cb4e8dfca3d`）。
- publish-product --publishType draft（taskId=238234390）终态 status=2 全 success，saveDraft 返回唯一草稿 1601924792152。
- search 回读发现平台 AI 改写/新增：标题被改、Model Number 丢斜杠（BQ040 1689）、Style=Casual、Lining=None、Midsole=MD Foam（AI 推断）、Feature×3（Lightweight/Slip On/Segmented Sole）。经 product-edit-draft-basic-info 两轮纠偏：标题改回确认版；删除 Style/Lining=None/Midsole=MD Foam/Feature×3；Lining 与 Midsole 补系统值 Other（valueId 4，用户指令：无精确值用 Other）；ADD Closure Type=Slip-On（13325270）。Model Number 属性 ADD/EDIT 含斜杠值触发接口"系统转换异常"，经 DELETE 后以 ADD+attrValueId 方式写回 BQ040 / 1689；Outsole=MD Foam、Toe/Upper/Origin 正确保留。关键词直接用空格分隔（无逗号拦截）。
- 详情图补写：list-information 显示 detail 组件无 detailImage → product-edit-draft-detail 用 operationType=ADD + newImageUrl + imageIndex 写入 6 张，回读确认落值（productDescType=4）。
- publish-draft --productId 1601924792152 --yes 返回 success:true, data=1601924792152，预检一次通过即发布；发布后 search 返回空（草稿已消费）、list-id 为空（审核队列中）。
- 当次在线总数 39（已含审核上线的 BQ039），BQ040/1689 在线匹配 0；审核通过后应变为 40。未调用 publish-product 直发，未生成第二个 BQ040，未改动 BQ032-BQ039。
- 接口限制：list-information 的 sku 组件返回空（21 SKU、3 颜色图无法直接回读），完整性由素材组装成功 + publish-draft 预检通过间接证明；Packaging=Standard Shoe Box 为服务模块属性（源自填写表 Packing Method），API 无法删除，非虚构宣称；Model Number 属性 ADD/EDIT 斜杠值接口"系统转换异常"，需 DELETE+ADD(attrValueId) 绕过。
- BQ033 重复草稿 1601924438053 仍为草稿态，按用户指示保留，未操作。
