# 工作台整理结果

日期：2026-09-11。本次按负责人明确授权整理现有文件、记忆、Skills 与 Git；原始商品、客户记录及有效执行证据不因日期旧而删除。

本次元数据盘点覆盖可访问的 351,139 个文件、约 44.51 GiB（含运行依赖和 E 盘链接资产，排除 Git 历史）；跳过 1,094 个重复目录目标。扫描中另记录 275 次依赖路径不存在异常：OpenCut 旧位置链接 78 条、独立站 pnpm 依赖链接 197 条，视频项目未发现异常；后续判断见[盘点异常说明](盘点异常说明.json)，统计不是冻结快照。

## 当前结构与 AI 入口

- 根 `AGENTS.md` 改为项目定位、工作目录、必要事实边界和条件式路由。Alibaba 与视频专项说明进入各自目录，不再在所有任务中加载完整技能清单与产物模板。
- README、资产索引、知识入口、视频入口分工明确；跨渠道总览删除过时状态和旧优先级，市场研究移除“44款缺口”等失效目标。独立站入口从 14,697 压缩到 3,148 字节，16 个历史发布哈希及验收证据均已确认由原手册/SOP 承载。
- 增加[工作区维护](../工作区维护.md)与[只读检查脚本](../../08_工具链/06_工作区维护/check_workspace.py)，检查目录、活动链接、记忆、Skills、Junction 和 Git 边界。
- 独立模拟普通视频、HOLD_SOURCE 报价、H3 参数、记忆整理与小幅标题修改五个情景，修复技术询盘触发词遗漏和视频入口无关加载。保留货源核验、样品边界、付费生成授权及中央 H3 规范。

## 记忆

| 项目 | 整理前 | 整理后 |
| --- | ---: | ---: |
| 正式记忆 | 24 | 13 |
| 记忆库文件 | 46 | 27 |
| 总字节 | 47,359 | 21,757 |
| 启动 README + INDEX 字节 | 9,422 | 5,527 |

全库缩减 54.1%，启动索引缩减 41.3%。合并公司与方向、货源上下文、冷邮件状态与复盘、人物混剪经验；删除重复 SOP、技能/资产路由、五层方法摘要和空分类分支。保留已确认偏好与有用经验，没有把旧实例或实验状态改写成今天已验证。

记忆读取按任务筛选，同任务不重读未变更内容；日常新增和经验晋级仍需确认，明确授权的维护可直接执行。七字段、两层目录、索引覆盖与原始来源已检查。

## Skills

- 原项目 12 个 Remotion 入口合为 1 个，删除 164 个重复文本/源码文件。保留项目 4.0.522 中比插件 4.0.506 更新的字幕、交互、HtmlInCanvas 等知识。
- 16 个只服务贝强的个人 Skills 整包迁到 `.agents/skills/`，原全局入口移除。迁移前后 50 个文件、130,809 字节逐文件 SHA-256 一致；项目最终有 21 个有效入口，属于归位与去重后的单一安装。
- 精简定位、国际站、CompShare 与记忆入口；长流程按需读取。修正定位卡无条件使用 wide toe/EVA/knit 的歧义，小任务不强制整套交付模板。
- OpenAPI 的 token 更新说明与实际显式 `refresh-token` 行为对齐。保留 API 权限、货源核验与唯一 SOP 路由。
- 通用工具和插件维持原安装方式；Workctl 两个个人入口的优先路由写入[Skills 清单](../../07_知识库与Skills/03_Skills清单.md)，两个一致的个人通用安装保留兼容，本项目只调用优先入口。

## 文件归位与清理

- AI 基础模型解释工程从根 `ai_foundation_research/` 整包迁到通用 AI 实验的在制目录。共 19,187 个文件、899,272,136 字节；迁后逐文件长度核对通过，PROJECT.md 哈希一致；迁移后的工程 ESLint 与 TypeScript 检查通过。工程和媒体仍保留本地，不将“已渲染”写成“用户已验收”。见[迁移记录](视频工程迁移.json)。
- BQ033、BQ036、BQ037 三份正式发品/草稿回执从临时区迁入 `02_Alibaba运营/03_发布记录/`，加历史标签；对应 analysis 证据保留。
- 两个商品图片工具脚本归入 `08_工具链/01_业务脚本/商品图片与审计/`；迁移哈希一致，语法检查通过。
- 删除失效目录建议、旧目录规则、两份巨大旧盘点、空候选 JSON、完成使命的迁移脚本和已被结构化审计取代的日志。此部分实际删除 16 个文件、34,830,792 字节，另有记忆和 Skills 去重；没有创建过时内容备份。见[逐项结果](清理执行结果.json)。

## 运行依赖位置修复

全量盘点的 275 次异常已逐组定位：OpenCut 78 条链接仍指向旧根 tools/ 路径；独立站 197 个缺失目标全部列在 pnpm 的 skipped 元数据，属于被跳过的可选依赖，不是业务原件遗失。

在 OpenCut 新目录按现有锁文件执行禁安装脚本、冻结锁文件的依赖修复后，原 78 条链接全部有效，旧路径引用归零，锁文件 SHA-256 不变。包管理器仍返回 exit 1：根包 opencut@root: 自引用链接出现 EPERM；具体系统原因未证实，因此只确认路径修复，不声称整次安装或应用功能验收通过。详细回执见[盘点异常说明](盘点异常说明.json)。

## Git 与验证

远端 `421345308/beiqiang-cross-border-operations` 已通过本机认证核实为私有仓库且有写入权限。本轮将当前工作区有效文本、自研脚本、历史业务回执及整理变更一并纳入提交；此前未提交内容没有被重置。

扩展忽略规则，排除浏览器登录态、第三方采集运行态、媒体、依赖、模型和压缩备份。既有被跟踪的素材/Excel 退出根 Git 索引，本地文件保留；独立站嵌套仓库不纳入根提交；当前另有 5 个已修改文件和 3 个未跟踪文件，属于原有购买指南功能开发，本次保留未提交，由独立站任务验证后再发布。清除一个 9 月 7 日遗留、无活动 Git 进程持有的空 index.lock 后恢复正常提交操作。

新增 .gitattributes 统一 Git 文本换行。维护代码与本轮入口的空白检查通过；历史原始 HTML/XML 抓取和 Markdown 模板仍保留其原有空白格式。

具体检查范围与结果见[验证报告](验证报告.json)、[提交前文本检查](提交前检查.json)、[目录规模](目录规模.json)。秘密信息检查只记录路径/结果，不输出凭证值；它是启发式检查，不替代人工保密判断。商品线上状态、客户回复或视频最终验收没有在此次文件治理中重新验证。

## 自动审批阻止的残留

自动批准审查拒绝了递归清理，以及技能重复 PNG 的固定路径删除，仅返回 `blocked by policy`，未给更细原因。可审阅文本补丁已完成；没有换通道绕过二进制删除拒绝。

仍有 24 个重复技能图标、6 个 pyc 缓存（151,096 字节），以及 10 张可重建 PDF 页和 1 份重复 PDF（2,047,024 字节），另有空目录。重复 Skill.md 已移除，残留不再被发现为技能；维护检查将其显示为明确警告。它们没有计入已删除数量。

运行中的 `_codex_work/`、原始商品、正式回执、客户档案和视频源/成片继续保留。Comfy 历史数据包内含 101 个素材/配置文件，尚未证明已被现有资料完整替代，因此保留作恢复证据并排除 Git；其中旧 H3 工作流不能作为新任务规范。
