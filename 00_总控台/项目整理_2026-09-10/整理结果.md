# 项目文件整理结果

日期：2026-09-10

## 整理后的主结构

根目录现在只保留总控、产品、渠道、客户、内容、研究、知识、工具、归档和临时区等一级入口。原先散落在根目录的 AI 视频工程、Alibaba 批次输出、临时运行目录、提示词、试图和 OpenCut 实体目录均已归位。

唯一暂留的非业务一级目录是 `_codex_work/`：盘点时发现多个 Blender MCP 进程正从该目录运行，因此没有强行移动或删除。它是运行环境，不是项目交付目录。

## AI 视频重新分层

`05_内容与视频/03_通用AI视频实验/` 已分为：

- `01_在制项目/`：正在生成、剪辑或等待验收。
- `02_已完成项目/`：已有正式成片并完成项目验收。
- `03_暂停项目/`：有明确暂停状态和恢复条件。
- `04_参考视频研究/`：原片、抽帧、镜头拆解和参考报告。
- `90_历史与待补档案/`：历史实验或缺少完整项目档案。

用户指出的抖音视频 `7674550636700011810.mp4` 已连同其抽帧、检查记录和《AI叙事视频参考拆解报告》整体迁入；后续又补齐同系列第 1、3、4、5 集，现统一归入：

`05_内容与视频/03_通用AI视频实验/04_参考视频研究/抖音_AI叙事系列参考_2026-09/`

没有只移动原片而拆散证据链；包内按视频工作流整理为 `edit/downloads/`、`edit/reports/`、`edit/qa/<作品ID>/`、`PROJECT.md` 和 `edit/project.md`。原片按第 1–5 集命名，逐集分析、系列横向报告与抽帧证据均保留。此前自动检查目录中 42 张与正式抽帧完全同哈希的 `_cache` 图片已删除。

## 已删除

以下内容有明确的“损坏、已删除、已归位、重复或可重建”证据，已直接删除且未另存备份：

- `08_工具链/02_视频工具/remotion_node_modules_broken_20260909/`：损坏的 Remotion 依赖副本，约 1.30 GB。
- `08_工具链/02_视频工具/remotion_deleted_20260909/`：已被当前 Remotion 工程替代的隔离旧工程，约 298 MB。
- `dunhuang_project_staging/.venv_bpy/`：无进程引用、可重建的旧 Python 运行环境，约 727 MB。
- `99_临时区/旧输出入口_已归位_2026-08-27/`：已归位空壳。
- 根目录空壳 `02_可上传素材/`、`outputs/新建文件夹/`。
- 通用 AI 视频目录中的空目录 `classical_chinese/`。
- 通用 AI 视频目录中的旧 `video-project-memory` 技能副本；当前统一使用已安装的 AI 视频学习系统。
- `output/seedance/` 兼容 Junction；只删除入口，正式视频未删除。

本轮明确删除约 2.32 GB、超过 2.3 万个可重建或被替代文件。

## 已归位

- `dunhuang_project_staging/` 中除旧运行环境外的早期方案、白模、提示词和画布资料，归入敦煌神女项目的 `90_早期琉璃天乐预演_2026-09-07/`。
- 根目录敦煌 9:16 提示词归入敦煌项目 `prompts/`。
- 根目录“风起琉璃”试图已在项目记录中被否定，归入“听弦起舞”的 `assets/rejected/`，作为项目失败证据保留。
- 根目录 `outputs/` 的 Alibaba 历史批次包归入 `02_Alibaba运营/05_扩品工程/批量发品/历史批次执行产物_2026-08-28至09-02/`，并修复引用路径。
- 根目录 `.tmp/` 与 `tmp/` 分别归入 `99_临时区/01_Alibaba临时运行/` 和 `99_临时区/04_运行依赖与检查缓存/`。
- OpenCut 实体目录由根 `tools/` 迁入 `08_工具链/02_视频工具/opencut-classic/`，根 `tools/` 已删除。
- 仍引用旧 `02_可上传素材/` 的当前 Alibaba 文档已改为 `01_产品资产/02_可发布素材/`。

## 保留边界

没有删除产品原始数据包、客户记录、正式业务回执、AI 视频正式成片或仍可能承担恢复证据的 Alibaba 临时数据。`99_临时区/` 中日期较早的内容不能仅凭“旧”字删除，仍需逐项核对正式回执和活动引用。

本轮目录判断遵循项目记忆中的“单一事实源、明确过时直接删除、未确认业务证据不擅删”原则；AI 视频按项目档案、经验候选、共享知识、技术运行环境分层。
