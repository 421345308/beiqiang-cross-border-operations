# C 盘整理记录（2026-08-28）

## 结果

- 整理前 C 盘可用空间：约 14.47 GB。
- 整理后 C 盘可用空间：约 29–30 GB（系统实时数值会有小幅波动）。
- 共增加约 15 GB 可用空间。
- 正式产品资料、商品视频、AI 视频实验均保留，原项目路径继续可用。

## 已迁移到 E 盘并保留原路径入口

| 原访问路径 | 实际存储路径 |
|---|---|
| `C:\Users\spq\Desktop\贝强\01_产品资产` | `E:\贝强大文件\01_产品资产` |
| `C:\Users\spq\Desktop\贝强\05_内容与视频\01_贝强商品视频` | `E:\贝强大文件\05_内容与视频\01_贝强商品视频` |
| `C:\Users\spq\Desktop\贝强\05_内容与视频\03_通用AI视频实验` | `E:\贝强大文件\05_内容与视频\03_通用AI视频实验` |

以上 C 盘路径现在是 Windows 目录联接（Junction）。日常仍从原路径打开，不需要修改脚本或工作习惯。不要单独删除 E 盘目标目录，否则原入口会失效。

## 移入可恢复备份

备份根目录：`E:\贝强_C盘整理备份_20260828`

- 网站、OpenCut 和工作区的 `node_modules` 已移至 E 盘，并在原位置建立目录联接，现有开发命令可继续使用；临时目录的依赖只保留备份。
- Git 失败操作产生的 25 个 `tmp_obj_*` 临时文件：不是有效版本历史。
- 已被业务验收否决的静态商品轮播视频批次。
- 带第三方品牌纸箱画面的旧工厂母版。
- 原工作区 `tmp` 内容；C 盘已重新建立空的 `tmp` 目录。

不要直接删除整个 E 盘备份目录：其中 `reinstallable` 下的三个主要依赖目录目前仍由 C 盘入口使用。若以后要删除，应先重新安装依赖到其他位置并移除对应目录联接；`rejected`、`git_incomplete_temp_objects` 和 `workspace_tmp` 可在确认不需恢复后单独清理。

## 未处理内容

- `C:\Users\spq\.codex\sessions`：约 6.37 GB，为 Codex 历史任务数据，未清理。
- 浏览器用户数据和缓存：未清理，避免退出登录或影响现有国际站会话。
- Git 中已有的工作区改动：未恢复、未覆盖、未提交。

## 验证

- 三个目录联接均可访问。
- 56 款商品视频对应表可访问。
- 无第三方标识的完整工厂视频可访问。
- 产品原始数据包可访问。
- 迁移目录未引入新的已跟踪文件删除。
